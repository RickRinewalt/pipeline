name: TOC Generator
on:
  push:
    paths:
      - 'README.md'
  workflow_dispatch:  # This allows manual runs
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
